{
  "name": "tradingview-news-scraper",
  "version": "1.0.0",
  "description": "Scrape TradingView News Flow (crypto) and publish feed.json + diff.json",
  "main": "src/generate-feed.js",
  "scripts": {
    "start": "node src/generate-feed.js"
  },
  "dependencies": {
    "puppeteer": "^21.0.0",
    "node-fetch": "^3.3.2"
  }
}
